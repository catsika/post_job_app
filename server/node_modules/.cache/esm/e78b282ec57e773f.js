let Job,User,cloudinary;_c57‍.x([["publishJob",()=>publishJob],["getAllJobs",()=>getAllJobs],["uploadPFP",()=>uploadPFP],["updatePFP",()=>updatePFP],["updateProfile",()=>updateProfile],["deleteJob",()=>deleteJob],["getJob",()=>getJob],["getMyJobs",()=>getMyJobs]]);_c57‍.w("../Models/Job.js",[["default",["Job"],function(v){Job=v}]]);_c57‍.w("../Models/Auth.js",[["default",["User"],function(v){User=v}]]);_c57‍.w("cloudinary",[["default",["cloudinary"],function(v){cloudinary=v}]]);



cloudinary.config({
  cloud_name: process.env.CLOUD_NAME,
  api_key: process.env.CLOUD_KEY,
  api_secret: process.env.CLOUD_SECRET,
});

       const publishJob = async (req, res) => {
  try {
    const {
      jobTitle,
      jobDescription,
      currentJobCat,
      subCategory,
      jobTags,
      imgUrl,
    } = req.body;
    const job = new Job({
      jobTitle,
      jobDescription,
      jobCategory: currentJobCat,
      jobSubcategory: subCategory,
      jobTags,
      imgUrl,
      postedBy: req.auth._id,
    });
    await job.save();
    res.json(job);
  } catch (error) {
    _c57‍.g.console.error(error);
  }
};

       const getAllJobs = async (req, res) => {
  try {
    const currentPage = req.params.page || 1;
    const perPage = 20;
    const jobs = await Job.find()
      .skip((currentPage - 1) * perPage)
      .sort({ createdAt: -1 })
      .limit(perPage)
      .populate(
        'postedBy',
        'fullname fulladdress.country image email createdAt'
      );
    res.json(jobs);
  } catch (error) {
    _c57‍.g.console.error(error);
  }
};

       const uploadPFP = async (req, res) => {
  try {
    const result = await cloudinary.uploader.upload(req.files.image.path);
    res.json({
      url: result.secure_url,
      public_id: result.public_id,
    });
  } catch (err) {
    _c57‍.g.console.log(err);
  }
};

       const updatePFP = async (req, res) => {
  try {
    const user = await User.findByIdAndUpdate(
      req.auth._id,
      { image: req.body },
      {
        new: true,
      }
    );
    user.password = undefined;
    res.json(user);
  } catch (error) {
    _c57‍.g.console.log(error);
  }
};

       const updateProfile = async (req, res) => {
  try {
    const { email, ctrystate, zip, city, address, country } = req.body;

    const user = await User.findOne({ email });
    if (user) {
      return res.json({
        error: 'An account with the specified email exists.',
      });
    }
    const user1 = await User.findByIdAndUpdate(
      req.auth._id,
      {
        email,
        fulladdress: {
          address,
          city,
          state: ctrystate,
          zip,
          country,
        },
      },
      {
        new: true,
      }
    );
    user1.password = undefined;
    res.json(user1);
  } catch (error) {
    _c57‍.g.console.log(error);
  }
};

       const deleteJob = async (req, res) => {
  try {
    const { postID } = req.body;
    const job = await Job.findByIdAndDelete(postID);
    if (job) res.json({ info: 'Success' });
  } catch (error) {
    _c57‍.g.console.log(error);
  }
};

       const getJob = async (req, res) => {
  const { _id } = req.params;
  const job = await Job.findById(_id).populate(
    'postedBy',
    'fullname fulladdress.country image email createdAt'
  );
  try {
    res.json(job);
  } catch (error) {
    _c57‍.g.console.log(error);
  }
};

       const getMyJobs = async (req, res) => {
  const _id = req.auth._id;
  const job = await Job.find({ postedBy: _id }).populate(
    'postedBy',
    'fullname fulladdress.country image email createdAt'
  );
  try {
    res.json(job);
  } catch (error) {
    _c57‍.g.console.log(error);
  }
};
